{
  "scripts": {
    "updated": "lerna updated",
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "format": "prettier --write \"**/*.{js,json,md}\""
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^4.2.0",
    "lerna": "^3.16.4",
    "lint-staged": "^9.2.5",
    "prettier": "^1.14.3"
  }
}
